<?xml version="1.0" encoding="utf-8"?>

<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.challenges.UserChallengesActivity">

    <include layout="@layout/toolbar_dark" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/app_background"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/framelayout_userchallenges_content"
            android:layout_width="match_parent"
            android:paddingTop="?attr/actionBarSize"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <android.support.design.widget.BottomNavigationView
            android:id="@+id/bottomnav_userchallenges"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            app:itemBackground="?attr/colorPrimary"
            app:itemIconTint="@color/user_challenges_menu_icon_tint"
            app:itemTextColor="@color/user_challenges_menu_text"
            app:menu="@menu/user_challenges_bottom_bar_menu" />

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>